#!/bin/bash
MY_PATH="`( cd \"$MY_PATH\" && pwd )`"
mkdir -p $MY_PATH/data
cp -n $MY_PATH/bin/settings.json.bak $MY_PATH/data/settings.json
mkdir -p $MY_PATH/data/irclogs
echo "all done, now edit the file inside $MY_PATH/data/settings.json"