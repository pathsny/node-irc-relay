Simple IRC relay that connects two irc channels.

To install, install node.js and npm

then
  npm install

copy settings.json.bak to data/settings.json and put in the right data! 


other dependencies by now
libicu-dev
libexpat1-dev
